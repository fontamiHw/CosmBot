{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [{
        "label": "copy-config-files",
        "command": "cp", // Could be any other shell command
        "args": ["${workspaceFolder}/CosmWebex-debug-config.yaml", "/data/COSM-bot/resources/CosmWebex-config.yaml"],
        "type": "shell"
    }]
}